package com.example.tourismapp.models;

import java.util.ArrayList;

class UserData {
    private ArrayList<UserInfo> userInfoData;

    UserData(ArrayList<UserInfo> userInfoData) {
        this.userInfoData = userInfoData;
    }
}